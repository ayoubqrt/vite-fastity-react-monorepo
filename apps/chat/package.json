{
	"name": "chat",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite --host",
		"build": "tsc && vite build",
		"start": "node dist/main.js",
		"docker:build": "docker build ../../ -f ./dockerfile -t neighbook_chat"
	}
}
